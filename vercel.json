{
  "buildCommand": "cd chat && npm install && npm run build && cd .. && mkdir -p dist && cp -r lander/* dist/ && mkdir -p dist/chat && cp -r chat/dist/* dist/chat/",
  "outputDirectory": "dist",
  "framework": null,
  "functions": {
    "api/chat.js": {
      "maxDuration": 60
    },
    "api/models.js": {
      "maxDuration": 30
    },
    "api/**/*.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/chat/(.*)", "destination": "/chat/index.html" }
  ]
}
